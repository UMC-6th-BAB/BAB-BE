spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bab
    username: root
    password: khsoo2580
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: update
        default_batch_fetch_size: 1000
security:
  jwt:
    token:
      secret-key: ${SECURITY_JWT_TOKEN_SECRET_KEY}
      expire-length: ${SECURITY_JWT_TOKEN_EXPIRE_LENGTH}
  oauth2:
    client:
      registration:
        kakao:
          client-id: ${OAUTH2_KAKAO_CLIENT_ID}
          redirect-uri: ${OAUTH2_KAKAO_REDIRECT_URI}
          authorization-grant-type: ${OAUTH2_KAKAO_AUTH_GRANT_TYPE}
